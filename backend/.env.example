# Groq API (for extraction.py)
GROQ_API_KEY=
# Optional — override if the default model ever gets retired by Groq (check
# https://console.groq.com/docs/models for currently supported model IDs;
# a 404 model_not_found error from gmail_ingest.py means this needs updating)
GROQ_MODEL=

# Supabase (Project Settings > API)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Gmail: comma-separated list of every enquiry-receiving Gmail address
GMAIL_ACCOUNTS=default
# Which mail to pull each poll (Gmail search syntax) — default for every
# account that doesn't have its own override below
GMAIL_QUERY=is:unread
# Optional: per-account query overrides as a JSON object, for an inbox
# that's mixed personal + business mail rather than a dedicated business
# address. Scope it to a Gmail label you've filtered business mail into:
# GMAIL_QUERY_OVERRIDES={"rahul@personal-domain.com": "label:Business is:unread"}
GMAIL_QUERY_OVERRIDES=

# WhatsApp webhook verification (set this to any string you choose,
# then enter the same string in Meta's webhook setup screen)
WHATSAPP_VERIFY_TOKEN=change-me
# Required. The phone_number_id (from Meta's payload metadata, not the
# phone number itself) of the dedicated NPD WhatsApp Business number —
# messages arriving on this number are routed to npd_updates instead of
# the customer-enquiry pipeline.
NPD_PHONE_NUMBER_ID=
# Optional. Meta Graph API access token (system user token, not a
# temporary one) for handle_npd_message() to send confirmation/
# clarification replies back on WhatsApp, and to download NPD voice note
# media for transcription. Without it, replies are just logged instead of
# sent, and voice notes are flagged for manual review instead of
# transcribed — typed-text inbound ingestion still works either way.
WHATSAPP_ACCESS_TOKEN=
# Optional — override if Meta retires the default Graph API version.
WHATSAPP_API_VERSION=
# Optional — override if the default Whisper model is ever retired by Groq
# (same reasoning as GROQ_MODEL above).
GROQ_WHISPER_MODEL=

# Daily digest (daily_digest.py) — summarizes overdue / due-soon / undated
# enquiries by email once a day.
# Must be one of the accounts already authorized in GMAIL_ACCOUNTS.
DIGEST_FROM_ACCOUNT=
# Comma-separated list of who should receive the digest
DIGEST_RECIPIENTS=
# Optional — included as a link at the top of the digest email
DASHBOARD_URL=
